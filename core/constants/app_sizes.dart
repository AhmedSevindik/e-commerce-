class AppSizes {
  AppSizes._();

  // ── Buton ──────────────────────────────────
  static const double buttonHeightLg = 56.0;
  static const double buttonHeightMd = 48.0;
  static const double buttonHeightSm = 36.0;
  static const double buttonHeightXs = 28.0;

  // ── Input ──────────────────────────────────
  static const double inputHeight    = 52.0;

  // ── Bar Yükseklikleri ──────────────────────
  static const double appBarHeight      = 56.0;
  static const double navbarHeight      = 60.0;
  static const double bottomNavHeight   = 72.0;

  // ── Ürün Kartı ─────────────────────────────
  static const double productCardWidth       = 160.0;
  static const double productCardHeight      = 240.0;
  static const double productImageHeight     = 140.0;

  // Yatay ürün kartı (liste görünümü)
  static const double productCardHorizHeight     = 100.0;
  static const double productCardHorizImageWidth = 90.0;

  // ── Banner / Slider ────────────────────────
  static const double bannerHeight = 180.0;
  static const double bannerRadius = 16.0;

  // ── Chip ───────────────────────────────────
  static const double categoryChipHeight = 36.0;

  // ── Avatar ─────────────────────────────────
  static const double avatarSm = 32.0;
  static const double avatarMd = 40.0;
  static const double avatarLg = 56.0;
  static const double avatarXl = 80.0;

  // ── Thumbnail ──────────────────────────────
  static const double thumbnailSm = 48.0;
  static const double thumbnailMd = 64.0;
  static const double thumbnailLg = 80.0;

  // ── Diğer ──────────────────────────────────
  static const double badgeSize        = 18.0;
  static const double paymentCardHeight = 56.0;
  static const double dividerThickness = 0.5;
  static const double minTouchTarget   = 44.0; // Accessibility standardı
}
